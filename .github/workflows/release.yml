name: Release

permissions:
  contents: write

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:
    inputs:
      tag_name:
        description: "Release tag to build, for example v0.1.0"
        required: true
        type: string

jobs:
  validate:
    name: Validate release version
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.meta.outputs.tag }}
      version: ${{ steps.meta.outputs.version }}

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.tag_name || github.ref }}

      - name: Resolve release metadata
        id: meta
        shell: bash
        run: |
          TAG="${{ github.event_name == 'workflow_dispatch' && inputs.tag_name || github.ref_name }}"
          VERSION="${TAG#v}"
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - name: Verify version files match tag
        run: node tools/verify-release-version.mjs "${{ steps.meta.outputs.tag }}"

  frontend-gate:
    name: Frontend release gate
    runs-on: ubuntu-latest
    needs: validate

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ needs.validate.outputs.tag }}

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Test
        run: npm run test

  rust-gate:
    name: Rust release gate
    runs-on: ubuntu-latest
    needs: validate

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ needs.validate.outputs.tag }}

      - name: Install Tauri Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            patchelf

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt

      - name: Rustfmt
        working-directory: src-tauri
        run: cargo fmt --check

      - name: Test
        working-directory: src-tauri
        run: cargo test

  prepare-release:
    name: Prepare GitHub release
    runs-on: ubuntu-latest
    needs:
      - validate
      - frontend-gate
      - rust-gate
    outputs:
      release_id: ${{ steps.release.outputs.release_id }}

    steps:
      - name: Create release notes
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG_NAME: ${{ needs.validate.outputs.tag }}
          REPOSITORY: ${{ github.repository }}
        shell: bash
        run: |
          cat > release-notes.md <<'EOF'
          ## 安装提醒

          这是 MapX 的未签名测试版，需要手动下载安装。

          - macOS 可能提示 "MapX is damaged and can't be opened"。这是未签名、未公证测试版被 Gatekeeper 拦截导致的。把 MapX 拖入 Applications 后，可在 Terminal 执行：

            ```sh
            xattr -dr com.apple.quarantine /Applications/MapX.app
            open /Applications/MapX.app
            ```

          - Windows 可能出现 SmartScreen 提示，需要用户手动确认继续安装。
          - 百度地图能力需要在应用设置中配置自己的百度地图开放平台 AK。
          - MapX 数据保存在系统 app data 目录；升级前建议确认自动备份目录中已有近期备份。

          EOF

          gh api "repos/${REPOSITORY}/releases/generate-notes" \
            -f tag_name="${TAG_NAME}" \
            -f target_commitish="${TAG_NAME}" \
            --jq .body >> release-notes.md

      - name: Create or update GitHub release
        id: release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG_NAME: ${{ needs.validate.outputs.tag }}
          REPOSITORY: ${{ github.repository }}
        shell: bash
        run: |
          if gh release view "${TAG_NAME}" --repo "${REPOSITORY}" >/dev/null 2>&1; then
            gh release edit "${TAG_NAME}" \
              --repo "${REPOSITORY}" \
              --title "MapX ${TAG_NAME}" \
              --notes-file release-notes.md \
              --prerelease
          else
            gh release create "${TAG_NAME}" \
              --repo "${REPOSITORY}" \
              --title "MapX ${TAG_NAME}" \
              --notes-file release-notes.md \
              --prerelease \
              --verify-tag
          fi

          RELEASE_ID="$(gh release view "${TAG_NAME}" --repo "${REPOSITORY}" --json id --jq .id)"
          echo "release_id=${RELEASE_ID}" >> "$GITHUB_OUTPUT"

  build-macos-release:
    name: Build release (macOS)
    runs-on: macos-latest
    needs:
      - validate
      - prepare-release

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ needs.validate.outputs.tag }}

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install dependencies
        run: npm ci

      - name: Build unsigned macOS DMG
        run: npm run tauri:build

      - name: Upload macOS DMG
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG_NAME: ${{ needs.validate.outputs.tag }}
          VERSION: ${{ needs.validate.outputs.version }}
        shell: bash
        run: gh release upload "${TAG_NAME}" src-tauri/target/release/bundle/dmg/MapX_${VERSION}_*.dmg --clobber

  build-windows-release:
    name: Build release (Windows)
    runs-on: windows-latest
    needs:
      - validate
      - prepare-release

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ needs.validate.outputs.tag }}

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install dependencies
        run: npm ci

      - name: Build and upload Windows MSI
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          releaseId: ${{ needs.prepare-release.outputs.release_id }}
          tagName: ${{ needs.validate.outputs.tag }}
          args: --ci --no-sign --bundles msi
          retryAttempts: 2
